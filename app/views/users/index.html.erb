<% provide(:title, 'All users') %>
<h1>お相手のユーザー</h1>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user unless current_user.active_likes?(user) %>
  <% end %>
</ul>

<!--%= will_paginate %-->